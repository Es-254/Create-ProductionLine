# 安全（多人服防注入）/ Security

> [← 总体 / Overview](../README.md) · 相关： [使用](usage.md) · [配方转换](conversion.md) · [兼容性与边界](compatibility.md) · [无头自检](qa.md)

## Security (multiplayer anti-injection, landed 2026-09-07) / 安全（多人服防注入，2026-09-07 落地）

**EN** — NBT-carrying items (schemes, mirrors) can be forged by modified clients on survival servers. So we rebuilt the mod around one rule: the server is the only authority. All findings below reference current sources, with `file:line` in parentheses.

- **Carrier whitelist.** `ClipboardCompat.isLoaderCarrier` (`compat/ClipboardCompat.java:83`) accepts only genuine `LineSchemeItem`, and both the loader slot (`menu/SchemeLoaderMenu.java:54-58`, the slot's `mayPlace` override) and the loader's own collection path (`block/entity/SchemeLoaderBlockEntity.java:220`, inside `currentEntriesMap` — which `reconcile()` calls at `:118` when it registers this cabinet's contribution) go through it; `maxRepeatAmongSlots()` applies the same check at `:137` while scanning the cabinet for the panel's repeat notice. Paper, clipboards, mirrors and forged NBT are all rejected.
- **Server-side re-derivation in the loader.** Activation never trusts the scheme's embedded JSON/Steps. `SchemeLoaderBlockEntity.currentEntriesMap` (`block/entity/SchemeLoaderBlockEntity.java:213-255`) resolves `RecipeId` via `ServerRecipeLookup.findById` (`line/mapper/ServerRecipeLookup.java:43`) to the **live recipe**, then `RecipeDeriver.derive` (`recipegen/RecipeDeriver.java:49`) re-derives the entries to install. A forged scheme can at most activate "a recipe that really exists on the server"; a missing or stale `recipeId` is skipped.
- **Authoritative dismantler refund.** `DismantlerBlockEntity.revert()` (`block/entity/DismantlerBlockEntity.java:140`) classifies slot 0 first: a **written Line Scheme** is erased into a fresh blank one (`:151-157` — no materials are involved, authoring a plan costs only the blank carrier), a **mirror** is refused outright (`:159-161`), and a plain `LineSchemeItem` with no plan is refused before anything is touched (`schemeCarriesContent`, `:293`). For an item it then validates in order: slot 1, when it holds something, must be a genuine `LineSchemeItem` — a mirror / paper / clipboard there is ignored rather than trusted (`:165-174`) → the `recipeId` (from that scheme, or resolved from slot 0's item when the slot is empty) must resolve server-side to a recipe with a usable output (`:209-215`) → slot 0's item id must equal that recipe's output (`:215-219`). A `#tag` in the refund set is materialized as the tag's first registered member, best effort (`materialize`, `:303-325`); if *nothing* in the set can be materialized, nothing is consumed at all (`:256-258`). It consumes first — one unit for an unfinished intermediate, a full inverse batch of `count` for a finished product (`:239-240`, `:263-269`) — then refunds (`:270`), then places the mirror (`:271-281`). The product itself stays in the refund set when the recipe consumes it, so `A + B = 2A` gives back `A + B` instead of eating an `A`; fluid ingredients can never be refunded (no item form exists) and are counted (`RecipeJsonReader.countFluidIngredients`) so the player is told rather than left guessing. The refund is a set of **unique materials, one each** — `2 planks -> 4 sticks` returns a single plank — which is deliberate: what makes dismantling non-farmable is the batch rule (`consume >= count`), not the size of the refund, and an unfinished intermediate still counts per deploy step so nothing it absorbed is lost.
- **No material is created by dismantling a plan.** A Line Scheme holds no materials — the computer only writes onto the carrier — so erasing one is a one-for-one swap (one written scheme out, one blank scheme in) and cannot be used to conjure carriers or materials. Unresolvable or mismatched items → **consume nothing, produce nothing**, which kills the "forge a scheme to print valuable materials" trick. Since the `1.0.3` dev line every refusal also names its reason to the player who pressed the button (the `RevertResult` enum, `:46-91`), so "nothing happened" is no longer ambiguous.
- **Network entry validation.** `ModPayloads.handleDismantle` (`network/ModPayloads.java:90`) calls `menu.stillValid(player)` before touching any slot.
- **Mirror is text-only.** `LineSchemeMirrorItem` (`item/LineSchemeMirrorItem.java:29,42`) writes only a `LineSchemeMirror` display snapshot (OutputItem/BaseMaterial/Steps text) with no executable `LineScheme`/embedded recipes, and `ClipboardCompat.isCarrier` returns `false` for it (`compat/ClipboardCompat.java:64-65`), so it can neither be activated nor re-fed.
- The computer generates server-side too, so validation is inherent, and it shares the same algorithm/entry point as the loader.
- **The placeholder question decides nothing by itself.** When a compute ends in "a live recipe that cannot become a Create line", the computer writes **nothing** and asks the requester privately whether to write a placeholder scheme (`block/entity/PlaceholderPrompt.java` holds the state and the rules, `event/CommandEvents.java` the click target, `menu/ComputerStatus.placeholderPrompt` the two clickable `RUN_COMMAND` options). The click carries no authority of its own: `/cpl placeholder accept|decline` takes no item, no target id and no position — only "who clicked, and what" — and the payload-free answer is then judged against state the block entity owns (`ProductionComputerBlockEntity.answerPlaceholderRequest`): the recorded asker's UUID must equal the clicker's, the permission level is re-read from the **live** player (2, the same gate as the anvil flow), and the deadline, the target slot and the open menu are re-read at that moment. A stranger's click (or one from a player whose permission was revoked) is `REJECTED` **and leaves the asker's question standing**, so it can be neither answered nor consumed by anybody else; the question is transient (never saved, cleared on load), and is dropped by the next compute, by a target-slot change, by the menu closing and after 30 s. What an accepted click can produce is the same inert object the mod always wrote: a scheme with an **empty `RecipeId`**, which the loader re-derives into nothing (`SchemeLoaderBlockEntity.entriesForSlot`) and counts as no filled slot, so the bar stays dark — the question only decides whether a *name to author against* exists, never a recipe.

**中文** — 方案、镜像这类"存 NBT 的物品"，在多人服上会被改包客户端伪造。为此我们按"服务端为唯一权威"重构了一遍（下面每条结论都对应现在的源码，括号里是文件:行）：

- **容器白名单**：`ClipboardCompat.isLoaderCarrier`（`compat/ClipboardCompat.java:83`）只认真 `LineSchemeItem`，加载柜槽位（`menu/SchemeLoaderMenu.java:54-58`，即槽位的 `mayPlace` 覆写）与加载柜自己的收集路径（`block/entity/SchemeLoaderBlockEntity.java:220`，位于 `currentEntriesMap` 内——`reconcile()` 在 `:118` 调它来登记本柜贡献）都走它；`maxRepeatAmongSlots()` 在 `:137` 用同一判定扫描柜内循环次数（供面板提示）。纸、剪贴板、镜像、随便伪造的 NBT 载体，一律拒收。
- **加载柜服务端推导**：激活时不信任方案内嵌的 JSON/Steps。`SchemeLoaderBlockEntity.currentEntriesMap`（`block/entity/SchemeLoaderBlockEntity.java:213-255`）按 `RecipeId` 调 `ServerRecipeLookup.findById`（`line/mapper/ServerRecipeLookup.java:43`）取到**实时配方**，再交给 `RecipeDeriver.derive`（`recipegen/RecipeDeriver.java:49`）重新推导该装哪些条目。伪造方案最多只能激活"服务器上真实存在的配方"；方案里没有的、失效的 `recipeId` 直接跳过。
- **破拆机权威退款**：`DismantlerBlockEntity.revert()`（`block/entity/DismantlerBlockEntity.java:140`）先给槽 0 分类：**写好的产线方案**清空成一份空白方案（`:151-157`——方案本身不含原料，做一份只花空白载体），**镜像**直接拒收（`:159-161`），**没有方案的普通方案物品**在动任何东西之前就拒绝（`schemeCarriesContent`，`:293`）。对物品则按顺序校验：槽 1 放了东西时必须是真 `LineSchemeItem`——放镜像/纸/剪贴板只会被忽略，不会被当成权威输入（`:165-174`）→ `recipeId`（来自该方案；槽 1 为空时按槽 0 物品反查）能在服务端解析出带可用产物的配方（`:209-215`）→ 槽 0 物品 id 必须等于该配方的产物（`:215-219`）。退还集合里的 `#tag` 会尽力折算成该标签的首个成员（`materialize`，`:303-325`）；只有当整个集合**一个都折算不出来**时才什么都不消耗（`:256-258`）。顺序是先消耗——未完成的中间产物消耗 1 个，成品按 `count` 整批消耗（`:239-240`、`:263-269`）——再退还（`:270`），最后放镜像（`:271-281`）。配方自己消耗的那份产物**保留在退还集合里**，所以 `A + B = 2A` 退的是 `A + B` 而不是只退 `B`；流体原料永远退不了（不存在物品形态），会统计项数（`RecipeJsonReader.countFluidIngredients`）并告知玩家。退的还是**去重后的原料集合**（每种一份）：`2 木板 → 4 木棍` 只退 1 块木板——这是有意为之，**防刷靠的是"整批消耗"规则**（`consume >= count`）而不是退款份数；未完成的中间产物按机械手步骤计数，它吸收过的原料一份不少。
- **拆方案不产生任何物资**：产线方案不含原料——计算机只是把内容写到载体上——所以"清空方案"是一换一（一份写好方案换一份空白方案），既不能凭空造载体也不能造原料。解析不到或产物不符 → **不消费不产出**，"伪方案刷贵重原料"这条路就被堵死了。`1.0.3` dev 线起，每种拒绝都会把原因私聊给按按钮的玩家（`RevertResult` 枚举，`:46-91`），"什么都没发生"不再无从判断。
- **网络入口校验**：`ModPayloads.handleDismantle`（`network/ModPayloads.java:90`）先做 `menu.stillValid(player)`，再触碰任何槽位，所以失效的 GUI 不会被执行。
- **镜像纯文本化**：`LineSchemeMirrorItem`（`item/LineSchemeMirrorItem.java:29,42`）只写 `LineSchemeMirror` 展示快照（OutputItem/BaseMaterial/Steps 文本），结构上不携带可执行 `LineScheme` 和内嵌配方，`ClipboardCompat.isCarrier` 对它直接返回 `false`（`compat/ClipboardCompat.java:64-65`），因此既不能被激活，也不能复喂。
- 产线计算机本身也是服务端生成，校验天然存在，和加载柜同源同算法。
- **占位方案的提问本身不握有任何权限**：当一次计算的结果是"存在实时配方但转不成 Create 产线"时，计算机**什么都不写**，而是私下询问发起者要不要写一份占位方案（状态与规则在 `block/entity/PlaceholderPrompt.java`，点击入口在 `event/CommandEvents.java`，两个可点击的 `RUN_COMMAND` 选项由 `menu/ComputerStatus.placeholderPrompt` 生成）。点击本身不带任何权威：`/cpl placeholder accept|decline` 不接受物品、不接受目标 id、不接受坐标，只回答"谁点的、点了什么"，而这个没有载荷的回答要拿去和方块实体自己持有的状态比对（`ProductionComputerBlockEntity.answerPlaceholderRequest`）：记录的提问对象 UUID 必须等于点击者，权限等级从**实时**玩家对象上重新读取（2 级，与铁砧流程同一道门），截止时间、目标槽内容与界面是否还开着也都在这一刻重新读。别人的点击（或权限被撤销者的点击）得到 `REJECTED`，并且**提问仍然保留在原提问者名下**——既答不了，也夺不走；该状态是瞬态的（从不存档，读档时清空），下一次计算、目标槽换物品、界面关闭、30 秒超时都会让它作废。而一次被接受的点击能产出的东西和以往完全一样：一份 `RecipeId` **为空**的方案——加载柜按它推导不出任何条目（`SchemeLoaderBlockEntity.entriesForSlot`）、也不计入已放格数，所以存储条保持熄灭。这个提问只决定"有没有一个可以照着写的目标名"，从不决定配方。

> **EN** — The same rule covers the compute entry point. The payload carries the real `recipeId`, and the server re-resolves it against its live `RecipeManager`, accepting it **only** when that recipe really produces the item sitting in the target slot. A client hint that cannot be verified yields no plan at all.
> **中文** — 计算入口也守同一条规则。计算包携带真 `recipeId`，服务端在实时 `RecipeManager` 上重新解析，**只有**当该配方确实产出目标槽内的物品时才采纳；验证不通过的客户端数据写不出任何方案。

> 返回 [总体 / Overview](../README.md)
